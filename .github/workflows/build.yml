name: Build My ImmortalWrt 24.10.2

on:
  workflow_dispatch:
    inputs:
      rootfs_size:
        description: '请输入固件根分区的最终大小 (MB)'
        required: true
        default: '2048'
      router_ip:
        description: '请设置路由器的默认管理IP地址'
        required: true
        default: '192.168.8.1'

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Maximize Build Space
        run: |
          echo "Cleaning up runner..."
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/lib/android /usr/local/share/powershell /opt/hostedtoolcache
          sudo docker image prune -a -f
          df -h

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential flex bison g++ gawk gcc-multilib g++-multilib \
          gettext git libncurses5-dev libssl-dev python3-distutils rsync unzip zlib1g-dev file wget

      - name: Clone My ImmortalWrt Source
        run: |
          # 使用您自己的 fork 仓库和 24.10 分支
          git clone --depth 1 -b openwrt-24.10 https://github.com/hyf0988/immortalwrt.git openwrt

      - name: Update and Install Default Feeds
        run: |
          cd openwrt
          # 只使用官方软件源，不添加任何第三方源
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: Generate Build Configuration
        run: |
          cd openwrt
          # 创建一个不包含不兼容插件的配置文件
          cat > .config << EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y
          # --- 强制生成完整大小的镜像 ---
          CONFIG_TARGET_IMAGES_PAD=y
          CONFIG_TARGET_ROOTFS_PARTSIZE=${{ github.event.inputs.rootfs_size }}
          # --- 禁用 ccache 以节省磁盘空间 ---
          CONFIG_ccache=n
          # --- 禁用 UPX 以避免编译错误 ---
          CONFIG_USE_UPX=n
          # --- 基础系统和软件包 ---
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-ssl-openssl=y
          CONFIG_PACKAGE_ca-bundle=y
          # --- Docker 支持 ---
          CONFIG_PACKAGE_luci-app-dockerman=y
          CONFIG_PACKAGE_docker=y
          CONFIG_PACKAGE_dockerd=y
          # --- 驱动和文件系统 ---
          CONFIG_PACKAGE_kmod-igb=y
          CONFIG_PACKAGE_kmod-e1000e=y
          CONFIG_PACKAGE_kmod-r8169=y
          CONFIG_PACKAGE_kmod-r8125=y
          CONFIG_PACKAGE_kmod-igc=y
          CONFIG_PACKAGE_kmod-usb-core=y
          CONFIG_PACKAGE_kmod-usb2=y
          CONFIG_PACKAGE_kmod-usb3=y
          CONFIG_PACKAGE_kmod-fs-ext4=y
          # --- 中文语言包 ---
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-dockerman-zh-cn=y
          EOF

          # 设置自定义IP地址
          echo "CONFIG_PACKAGE_base-files-custom-ip=y" >> .config
          echo "CONFIG_TARGET_CUSTOM_IP_ADDRESS='${{ github.event.inputs.router_ip }}'" >> .config
          
          # 完成配置
          make defconfig

      - name: Compile Firmware
        id: compile
        run: |
          cd openwrt
          # 使用单核编译以保证稳定性和低内存占用，过程会很长
          make -j1 V=s

      - name: Organize Files and Create Info
        id: organize
        if: success()
        run: |
          FIRMWARE_PATH=$(find ./openwrt/bin/targets/x86/64 -type f -name "*squashfs-combined-efi.img.gz" | head -n 1)
          if [[ -z "$FIRMWARE_PATH" ]]; then
            echo "::error::固件文件未找到, 编译失败!"
            exit 1
          fi
          echo "FIRMWARE_PATH=$FIRMWARE_PATH" >> $GITHUB_ENV
          
          cat > ./router-info.md <<EOF
          ## 🚀 ImmortalWrt 24.10.2 (精简版)

          - 🏠 **后台地址**: ${{ github.event.inputs.router_ip }}
          - 👤 **用户名**: root
          - 🔑 **密码**: 无
          - 💾 **固件大小**: ${{ github.event.inputs.rootfs_size }} MB
          EOF

      - name: Generate Release Tag
        id: tagger
        if: success()
        run: |
          echo "release_tag=ImmortalWrt-24.10.2-Build-$(date +%Y-%m-%d-%H%M)" >> $GITHUB_OUTPUT

      - name: Upload Firmware to Release
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          tag_name: ${{ steps.tagger.outputs.release_tag }}
          body_path: ./router-info.md
          files: |
            ${{ env.FIRMWARE_PATH }}
          token: ${{ secrets.GITHUB_TOKEN }}
